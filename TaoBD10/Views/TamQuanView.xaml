<UserControl x:Class="TaoBD10.Views.TamQuanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaoBD10.Views"
             DataContext="{StaticResource TamQuanVM}"
             mc:Ignorable="d" 
            >
    <StackPanel>
        <DataGrid AutoGenerateColumns="False" SelectionMode="Single" Height="200" ItemsSource="{Binding TamQuans}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Index" Binding="{Binding Index}"/>
                <DataGridTextColumn Header="Mã Hiệu" Binding="{Binding MaHieu}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Margin="10" Content="Send" Command="{Binding SendCommand}"/>

    </StackPanel>
</UserControl>
