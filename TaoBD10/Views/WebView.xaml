<UserControl x:Class="TaoBD10.Views.WebView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaoBD10.Views"
             DataContext="{StaticResource WebVM}"
             mc:Ignorable="d" 
             xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:cefSharpWpf="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
             >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="193*"/>
            <RowDefinition Height="32*"/>
        </Grid.RowDefinitions>
        <cefSharpWpf:ChromiumWebBrowser Grid.Row="0" x:Name="Browser" WebBrowser="{Binding WebBrowser,Mode=OneWayToSource}"
                                    Address="{Binding AddressWeb, Mode=TwoWay}"/>
        <Button Command="{Binding LoginCommand}"  Content="Button" HorizontalAlignment="Left"  Grid.Row="1" VerticalAlignment="Top" Width="75"/>

    </Grid>
</UserControl>
