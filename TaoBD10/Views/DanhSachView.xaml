<UserControl x:Class="TaoBD10.Views.DanhSachView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaoBD10.Views"
             DataContext="{StaticResource DanhSachVM}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView
  ItemsSource="{Binding BD10List}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="50" Header="Favourite" >
                        <GridViewColumn.CellTemplate>
                            <DataTemplate >
                                <CheckBox IsChecked="{Binding isChecked}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                       
                    </GridViewColumn>
                    <GridViewColumn
        DisplayMemberBinding="{Binding Name}"
        Header="Tên BD" />
                    <GridViewColumn
        DisplayMemberBinding="{Binding LanLap}"
        Header="Lần Lập" />
                    <GridViewColumn
        DisplayMemberBinding="{Binding CountTui}"
        Header="Số Lượng" />
                </GridView>
            </ListView.View>
        </ListView>       
    </Grid>
</UserControl>
